# Desktop package (Electron)

See [docs/DESKTOP.md](../docs/DESKTOP.md) for architecture, import, and build instructions.

```bash
npm install
npm run build
npm run dist   # Windows NSIS via electron-builder
```
